# Codex instructions

作業開始時に `.ai/INDEX.md` を全文読み、参照先の現行仕様と状態を確認してください。

- Codexの出所識別子は `ai:codex` です。
- 変更は作業ブランチとDraft Pull Requestで提案します。
- `main` への直接更新、Pull Requestの承認・マージ、削除、履歴書き換えは禁止します。
- このリポジトリはPublicです。privateな横断knowledge、資格情報、個人情報、ローカルパスを保存しません。
- `shared-ai-knowledge` を参照できる場合は `AI_GUIDE.md` を優先し、参照できない場合は対応済みと推測しません。
